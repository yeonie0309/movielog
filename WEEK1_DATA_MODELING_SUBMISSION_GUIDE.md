# 1주차 요구사항 데이터 모델링 제출 가이드

## 미션 기록에 붙여 넣을 내용

### 진행 과정

1. IA와 와이어프레임의 핵심 화면을 로그인/회원가입, 홈의 지역·가게·음식
   카테고리, 미션 목록·수행 내역으로 나눴다.
2. 화면에서 저장해야 하는 명사와 상태를 추출해 `members`, `social_accounts`,
   `regions`, `food_categories`, `stores`, `missions`, `member_missions`를 핵심
   엔티티로 도출했다.
3. 회원가입 약관·선호 음식, 미션 인증, 완료 후 리뷰·사진까지 와이어프레임을 다시
   검토해 `terms`, `member_terms`, `member_food_categories`, `reviews`,
   `review_images`를 추가했다.
4. 각 엔티티의 PK를 `id BIGINT AUTO_INCREMENT`로 통일하고, 필수 화면 데이터와
   선택 데이터를 구분해 `NOT NULL`을 설정했다.
5. 회원-미션의 N:M 관계를 `member_missions` 매핑 테이블로 풀고, 약관 동의와
   선호 음식도 각각 매핑 테이블로 정규화했다.
6. 중복 가입·중복 미션 참여를 막는 UNIQUE 제약과 상태 CHECK, FK, 조회용 인덱스,
   회원 탈퇴 및 운영 데이터 보존을 위한 Soft Delete를 추가했다.
7. SQL·DBML·Mermaid 산출물을 대조해 테이블명, 컬럼명, 타입, 관계를 검토했다.

### ERD 설명

- ERDCloud: https://www.erdcloud.com/d/N2Q539qkpjMmdgPpy

회원은 여러 소셜 계정과 여러 미션 수행 이력을 가질 수 있다. 지역에는 여러 가게가
속하고, 각 가게는 하나의 음식 카테고리로 분류되며 여러 미션을 제공한다. 회원과
미션의 N:M 관계는 수행·인증 상태와 시각을 기록해야 하므로 `member_missions`로
분리했다. 약관 동의와 선호 음식도 N:M 매핑으로 정규화하고, 완료한 미션에는 하나의
리뷰와 여러 장의 리뷰 사진을 연결할 수 있게 했다.
회원 탈퇴는 `members.deleted_at`을 사용하는 Soft Delete로 처리해 미션 수행 이력과
참조 무결성을 보존한다.

워크북에서 제외 가능하다고 한 지도·검색, 포인트 관리·알림 설정, 사장님 점포 관리
기능은 이번 범위에서 제외했다. 지역별 미션 10개 완료 여부는 회원 미션-미션-가게를
조인해 완료 건수를 집계할 수 있도록 설계했다.

### 검증 결과

- `flutter analyze`: 실행 결과를 PR 작성 시 기재
- `flutter test`: 실행 결과를 PR 작성 시 기재
- DB 설계 체크: 12개 테이블의 PK, FK, UNIQUE, CHECK, NULL 여부 및 Soft Delete 확인

## 트러블 슈팅 기록 예시

**`이슈`**

👉 서비스 요약에는 “지역별 미션 10개 완료 시 1,000 Point 지급”이 있지만,
미션 범위에서는 포인트 관리 기능을 제외할 수 있어 포인트 테이블 포함 여부가
모호했다.

**`문제`**

👉 보상 지급 내역까지 설계하면 필수 화면 범위를 넘어 포인트 원장, 적립·차감 유형,
중복 지급 방지 정책까지 추가로 결정해야 했다. 반대로 관련 데이터를 완전히 제외하면
지역별 완료 조건을 계산하기 어려울 수 있었다.

**`해결`**

👉 이번 ERD에서는 포인트 원장을 제외하되 `member_missions.status`, `missions`와
`stores.region_id`의 관계를 통해 회원별·지역별 완료 미션 수를 계산할 수 있게 했다.
포인트 지급 원장은 정책이 확정되는 다음 단계에서 별도 엔티티로 확장하도록 범위와
가정을 문서에 명시했다.

**`참고레퍼런스`**

- 레아의 워크북 「1주차 - 요구사항을 데이터로 바꾸기」의 필수 요구사항과 제외 대상

## 직접 해야 할 제출 작업

1. 생성된 ERDCloud 프로젝트에서 전체 12개 테이블과 관계선이 보이도록 PNG를
   다운로드하거나 화면을 캡처해 노션의 `ERD 사진`에 넣는다.
2. 위 `진행 과정`, `ERD 설명`, `트러블 슈팅 기록 예시`를 본인 미션 기록에 붙여 넣는다.
3. 노션 실습 체크리스트는 캡처와 기록까지 확인한 뒤 전부 체크한다.
4. 이 PR 링크와 ERDCloud 링크를 미션 기록에 추가한다.
5. 원본 워크스페이스의 개인 미션 페이지 공유 링크를 복사해 UMC AI 피드백 시스템에
   제출한다. 기술 블로그는 상점용 권장 사항이라 필수 제출은 아니다.
